To see the documentation:
Download the phpdoc folder or the phpdoc.zip file and extract it. Then open index.html and navigate through the documentation.

To generate the documentation run:
phpdoc -o HTML:frames:earthli --ignore protected/tests/php-webdriver/ -d "full\path\to\project" -t phpdoc -q -ti "MySurvey Application" 